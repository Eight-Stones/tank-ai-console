app.name = "example"

logger.level = "debug"
logger.keys.inst = "local"

controller.rest.host = "localhost"
controller.rest.port = 8081

controller.grpc.host = "localhost"
controller.grpc.port = 50052

gateway.rest.tank.host = "localhost"
gateway.rest.tank.port = 8080